# Maintainer: Filippov Aleksey <sarum9in@gmail.com>
pkgname=qemu-scripts
pkgver=0.4
pkgrel=1
epoch=
pkgdesc="QEMU start scripts"
arch=('any')
url=""
license=('GPL')
groups=()
depends=('uml_utilities' 'bridge-utils' 'qemu' 'sudo')
makedepends=()
checkdepends=()
optdepends=()
provides=()
conflicts=()
replaces=()
backup=('etc/qemu-scripts.conf')
options=()
install=qemu-scripts.install
changelog=
source=($pkgname-$pkgver.tar.xz)
noextract=()
md5sums=('4c6f56fe748e39d6e6a2417db1663a7e')

build() {
  cd "$srcdir/$pkgname-$pkgver"
}

#check() {
#  cd "$srcdir/$pkgname-$pkgver"
#  make -k check
#}

package() {
  cd "$srcdir/$pkgname-$pkgver"
  tar cf - . | (cd "$pkgdir" && tar xf -)
}

# vim:set ts=2 sw=2 et:
